package org.bitbuckets.lib.vendor.spark;

class LimitSwitchLoggerTest {
/*
    @Test
    void run() {

        ILoggable<Boolean> loggable = Mockito.mock(ILoggable.class);
        SparkMaxLimitSwitch sparkMaxLimitSwitch = Mockito.mock(SparkMaxLimitSwitch.class);

        LimitSwitchLogger logger = new LimitSwitchLogger(loggable, sparkMaxLimitSwitch);
        Mockito.when(sparkMaxLimitSwitch.isPressed()).thenReturn(true);

        logger.run();

        Mockito.verify(loggable).log(true);
    }*/
}